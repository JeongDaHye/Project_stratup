<%= bootstrap_form_for(@post7) do |f| %>
  <% if @post7.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post7.errors.count, "error") %> prohibited this post7 from being saved:</h2>

      <ul>
      <% @post7.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 <div class="field">
   
    <%= f.text_field :title, class:"form-control" %>
  </div>
  <div class="field">
    
    <%= f.text_area :content,class:"form-control" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
